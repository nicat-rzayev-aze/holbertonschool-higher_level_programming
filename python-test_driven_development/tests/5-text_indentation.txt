Importing the function:
    >>> is_same_class = __import__('1-is_same_class').is_same_class

Testing with integers:
    >>> a = 1
    >>> is_same_class(a, int)
    True
    >>> is_same_class(a, float)
    False
    >>> is_same_class(a, object)
    False

Testing with floats:
    >>> b = 3.14
    >>> is_same_class(b, float)
    True
    >>> is_same_class(b, int)
    False

Testing with strings:
    >>> is_same_class("Holberton", str)
    True
    >>> is_same_class("School", object)
    False
